/* 
1. Write a `while` loop that calculates the sum of all numbers from 1 to 5 and stores the result in a variable named `sum`.
*/

let sum = 0;
let i = 1;
while(i<=5) {
    sum = sum + i;
    i++
}      
console.log(sum)
/* 
2. Write a `while` loop that calculates the sum of all numbers from 1 to 6 and stores the result in a variable named `sum`.
*/
let sum1 = 0;
let g =1;
while (g<=6) {
    sum1 = sum1 + g;
    g++;
}
console.log(sum1)

/* 
3. Write a `while` loop that calculates the sum of all numbers from 1 to 7 and stores the result in a variable named `sum`.
*/
 let sum2 = 0;
 let f = 1;
 while (f<=7) {
    sum2 = sum2 + f;
    f++
 }
 console.log(sum2)



/* 
4. Write a `while` loop that calculates the sum of all numbers from 1 to 8 and stores the result in a variable named `sum`.
*/
let sum3 = 0;
 let e = 1;
 while (e<=8) {
    sum3 = sum3 + e;
    e++
 }
 console.log(sum3)


/* 
5. Write a `while` loop that calculates the sum of all numbers from 1 to 9 and stores the result in a variable named `sum`.
*/

let sum4 = 0;
 let d = 1;
 while (d<=9) {
    sum4 = sum4 + d;
    d++
 }
 console.log(sum4)

/* 
6. Write a `while` loop that calculates the sum of all numbers from 1 to 10 and stores the result in a variable named `sum`.
*/
let sum5 = 0;
 let c = 1;
 while (c<=10) {
    sum5 = sum5 + c;
    c++
 }
 console.log(sum5)



/* 
7. Write a `while` loop that calculates the sum of all numbers from 1 to 11 and stores the result in a variable named `sum`.
*/
let sum6 = 0;
 let b = 1;
 while (b<=11) {
    sum6 = sum6 + b;
    b++
 }
 console.log(sum6)


/* 
8. Write a `while` loop that calculates the sum of all numbers from 1 to 12 and stores the result in a variable named `sum`.
*/
let sum7 = 0;
 let a = 1;
 while (a<=12) {
    sum7 = sum7 + a;
    a++
 }
 console.log(sum7)

/* 
3. Write a `do while` loop that prompts a user to enter their favorite tea type until they enter `"stop"`. 
   Store each tea type in an array named `teaCollection`.
*/



//4. Write a `do while` loop that adds numbers from 1 to 3 and stores the result in a variable named `total`.



/* 
5. Write a `for` loop that multiplies each element in the array `[2, 4, 6]` by 2 and stores the results in a new array named `multipliedNumbers`.
*/


/* 
6. Write a `for` loop that lists all the cities in the array `["Paris", "New York", "Tokyo", "London"]` and stores each city in a new array named `cityList`.
*/
